global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: "fastapi"
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:8000"]

  - job_name: "renderer"
    metrics_path: /metrics
    static_configs:
      - targets: ["renderer:9092"]